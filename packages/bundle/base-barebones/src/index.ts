/**
 * @deepseek-ai/dsh-base-barebones — an opt-in low-egress overlay over
 * `@deepseek-ai/dsh-base`. The package's substance is `cordis.patch.yml`,
 * declared by the `dsh.bundle.patch` manifest field and resolved by the
 * profile composer through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-base-barebones
 */

export {}
